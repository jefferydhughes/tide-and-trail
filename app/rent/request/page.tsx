import { RequestForm } from '@/components/RequestForm'
export default async function RentalRequest({searchParams}:{searchParams:Promise<{kit?:string}>}){const{kit}=await searchParams;return <main className="container-x py-16"><div className="mx-auto max-w-xl"><span className="pill">Fundy Footpath rentals</span><h1 className="display mt-5 text-5xl text-forest">Reserve your trail kit.</h1><p className="mt-4 text-forest/60">Send your dates and we’ll confirm availability, sizing, pickup, and the final price before anything is booked.</p><div className="mt-8 rounded-3xl bg-white p-7 shadow-soft"><h2 className="mb-5 font-black text-forest">{kit||'Build Your Own'}</h2><RequestForm type="rental" packageName={kit}/></div></div></main>}
