'use client'
import { useState } from 'react'
export function ProductReservation({productId,available}:{productId:string;available:boolean}){const[open,setOpen]=useState(false),[done,setDone]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/products/reserve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productId,...Object.fromEntries(f)})});const j=await r.json();if(!r.ok){setError(j.error);return}setDone(true)}finally{setLoading(false)}}if(!available)return <p className="mt-7 rounded-2xl bg-sand p-5 font-bold text-forest">This item is currently on hold.</p>;if(done)return <div role="status" className="mt-7 rounded-2xl bg-sage p-5"><strong>Held for 24 hours.</strong><p className="mt-1 text-sm">We’ll email you to arrange pickup and payment.</p></div>;if(!open)return <button onClick={()=>setOpen(true)} className="btn-primary mt-7">Hold for local pickup</button>;return <form onSubmit={submit} className="mt-7 grid gap-3 rounded-2xl bg-white p-5 shadow-soft"><h2 className="font-black text-forest">24-hour pickup hold</h2><input name="name" required maxLength={120} aria-label="Name" placeholder="Your name" className="rounded-xl bg-fog px-4 py-3"/><input name="email" required type="email" aria-label="Email" placeholder="Email" className="rounded-xl bg-fog px-4 py-3"/><input name="phone" maxLength={40} aria-label="Phone" placeholder="Phone (optional)" className="rounded-xl bg-fog px-4 py-3"/>{error&&<p role="alert" className="text-sm font-bold text-red-700">{error}</p>}<button disabled={loading} className="btn-primary">{loading?'Holding…':'Confirm 24-hour hold'}</button></form>}
