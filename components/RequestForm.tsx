'use client'
import { useState } from 'react'
const input='rounded-2xl border border-forest/10 bg-fog px-4 py-3.5'
export function RequestForm({type,packageName}:{type:'rental'|'adventure';packageName?:string}){const[done,setDone]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const body=Object.fromEntries(f);try{const r=await fetch(type==='rental'?'/api/rentals':'/api/adventures',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok){setError(j.error);return}setDone(true)}catch{setError('Network error. Please try again.')}finally{setLoading(false)}}if(done)return <div role="status" className="rounded-2xl bg-sage p-6"><h2 className="display text-3xl text-forest">Adventure incoming.</h2><p className="mt-2 text-forest/65">We received your request and will be in touch.</p></div>;return <form onSubmit={submit} className="grid gap-4"><input name="name" required maxLength={120} aria-label="Name" placeholder="Your name" className={input}/><input name="email" required type="email" aria-label="Email" placeholder="Email" className={input}/><input name="phone" maxLength={40} aria-label="Phone" placeholder="Phone (optional)" className={input}/>{type==='rental'?<><input type="hidden" name="package" value={packageName||'Build Your Own'}/><div className="grid grid-cols-2 gap-3"><label className="text-sm font-bold">Start date<input name="startsOn" type="date" required className={`${input} mt-1 w-full`}/></label><label className="text-sm font-bold">End date<input name="endsOn" type="date" required className={`${input} mt-1 w-full`}/></label></div><input name="partySize" type="number" min="1" max="12" defaultValue="1" aria-label="Party size" className={input}/></>:<><input name="title" required maxLength={180} aria-label="Adventure title" placeholder="Adventure title" className={input}/><select name="adventureType" aria-label="Adventure type" className={input}><option>Hike</option><option>Paddle</option><option>Camp</option><option>Trail run</option><option>Other</option></select><input name="location" required maxLength={180} aria-label="Location" placeholder="General location" className={input}/><label className="text-sm font-bold">Date and time (optional)<input name="startsAt" type="datetime-local" className={`${input} mt-1 w-full`}/></label></>}<textarea name={type==='rental'?'notes':'description'} required={type==='adventure'} maxLength={2000} aria-label="Details" placeholder="Tell us the useful details…" className={`${input} min-h-28`}/>{error&&<p role="alert" className="text-sm font-bold text-red-700">{error}</p>}<button disabled={loading} className="btn-primary">{loading?'Sending…':type==='rental'?'Request this kit':'Submit for review'}</button></form>}
